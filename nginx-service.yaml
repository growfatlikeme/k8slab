apiVersion: v1
kind: Service
metadata:
  name: growfatlikeme-nginx-service
  namespace: growfatlikeme-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
