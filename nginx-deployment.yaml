apiVersion: apps/v1
kind: Deployment
metadata:
  name: growfatlikeme-nginx-deployment
  namespace: growfatlikeme-eks-activity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      serviceAccountName: growfatlikeme-nginx-service-account
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
